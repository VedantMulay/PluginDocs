# Message Configuration

Almost eveny message the plugin sends is configurable. The plugin also supports PlaceholderAPI placeholders in all of the messages.

## In-built placeholders

The plugin has some in-built placeholders that can be used in specific messages to add context to them.

### \{prefix\} placeholder
Adds the `prefix:` from the config file. Applicable to all of the messages in the plugin.
